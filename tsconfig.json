{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    // The seed script (scripts/seed-sanity.ts) runs on Node's native TS
    // support, which requires explicit .ts extensions in import paths.
    "allowImportingTsExtensions": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  // kie-mcp-0.5.3 is a vendored dev-time MCP connector, not part of this app —
  // exclude it so the type checker doesn't compile its (unbuilt) sources.
  "exclude": ["node_modules", "kie-mcp-*/"]
}
